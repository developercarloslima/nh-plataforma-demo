const TOKEN_KEY='nhPortalToken',CONSULTANT_KEY='nhSelectedConsultant';const $=id=>document.getElementById(id);const token=localStorage.getItem(TOKEN_KEY);const consultant=JSON.parse(localStorage.getItem(CONSULTANT_KEY)||'null');if(!token||!consultant?.id)location.replace('/colaborador/');
async function api(path,options={}){const h=new Headers(options.headers||{});h.set('Authorization',`Bearer ${token}`);const r=await fetch(path,{...options,headers:h});if(!r.ok){const b=await r.json().catch(()=>null);throw new Error(b?.message||'Não foi possível concluir a solicitação.')}return r.json()}
function msg(text,type='error'){const e=$('message');e.className=`message ${type}`;e.textContent=text}
$('consultant-info').textContent=`Consultor responsável: ${consultant.name}`;const q=new URLSearchParams(location.search);$('associateName').value=q.get('name')||'';$('plate').value=q.get('plate')||'';$('whatsapp').value=q.get('whatsapp')||'';
$('cpf').addEventListener('input',e=>{let v=e.target.value.replace(/\D/g,'').slice(0,11);e.target.value=v.replace(/(\d{3})(\d)/,'$1.$2').replace(/(\d{3})(\d)/,'$1.$2').replace(/(\d{3})(\d{1,2})$/,'$1-$2')});$('plate').addEventListener('input',e=>e.target.value=e.target.value.toUpperCase());
$('inspection-form').addEventListener('submit',async e=>{e.preventDefault();try{const data=await api('/api/inspections',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({consultantId:consultant.id,requestType:$('requestType').value,associateName:$('associateName').value.trim(),cpf:$('cpf').value,plate:$('plate').value.trim(),whatsapp:$('whatsapp').value})});$('result').hidden=false;$('public-link').value=data.publicUrl;$('open-link').href=data.publicUrl;$('send-whatsapp').href=data.whatsappUrl||`https://wa.me/?text=${encodeURIComponent(data.publicUrl)}`;$('result-summary').textContent=`${data.associateName} · ${data.plate} · link válido até ${new Date(data.expiresAt).toLocaleDateString('pt-BR')}`;$('result').scrollIntoView({behavior:'smooth'});msg('Solicitação registrada no painel administrativo.','success')}catch(err){msg(err.message)}});
$('copy-link').addEventListener('click',async()=>{await navigator.clipboard.writeText($('public-link').value);msg('Link copiado.','success')});$('new-request').addEventListener('click',()=>{location.href='/colaborador/retrato.html'});
